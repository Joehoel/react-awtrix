pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: "*.{ts,tsx}"
      run: bunx oxlint --fix {staged_files}
      stage_fixed: true
    - name: format
      glob: "*.{ts,tsx,json,md}"
      run: bunx oxfmt --write {staged_files}
      stage_fixed: true
